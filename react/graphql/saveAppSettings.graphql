mutation SaveAppSettings($version: String, $settings: String) {
  saveAppSettings(
    app: "vinneren.connector-digital-river"
    version: $version
    settings: $settings
  ) @context(provider: "vtex.apps-graphql") {
    message
  }
}
